# Web Dashboard - Дашборд для мониторинга порталов

Веб-приложение на Django для управления и мониторинга порталов с отслеживанием их доступности.

## 🚀 Возможности

- 📊 Дашборд с карточками порталов
- 🔄 Drag-and-drop сортировка
- 🌐 Автоматический сбор favicon
- 📈 Мониторинг доступности
- 👤 Аутентификация пользователей
- 📱 Адаптивный дизайн

## 🐳 Docker развертывание (продакшен)

### Быстрый старт

```bash
# Запуск скрипта развертывания
./deploy.sh
```

### Ручное развертывание

```bash
# Сборка образа
docker build -t web_dashboard:latest .

# Запуск контейнера
docker run -d \
    --name web_dashboard \
    --restart unless-stopped \
    -p 4213:4213 \
    -v $(pwd)/data:/app/data \
    -v $(pwd)/media:/app/media \
    -e DJANGO_SECRET_KEY="your-secret-key" \
    web_dashboard:latest

# Применение миграций
docker exec web_dashboard python manage.py migrate --settings=web_dashboard.settings_prod
```

### Доступ

- **Локальный:** http://localhost:4213
- **IP:** http://127.0.0.1:4213, http://155.212.166.17:4213
- **Домен:** http://dashbord.gegaremant.ru:4213

### Docker Compose

```bash
docker-compose up -d
```

## 💻 Локальная разработка

### Установка

```bash
# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Применение миграций
python manage.py migrate

# Запуск сервера
python manage.py runserver
```

### Создание суперпользователя

```bash
python manage.py createsuperuser
```

## 📁 Структура проекта

```
web_dashbord/
├── accounts/           # Приложение аутентификации
│   ├── views.py        # Логин и регистрация
│   └── urls.py         # URL маршруты
├── portals/            # Приложение порталов
│   ├── models.py       # Модели Portal, PortalAvailability
│   ├── views.py        # AJAX эндпоинты
│   ├── services.py     # Сервисы (favicon, проверка доступности)
│   └── admin.py        # Конфигурация админки
├── web_dashboard/      # Настройки проекта
│   ├── settings.py     # Основные настройки
│   ├── settings_prod.py# Продакшен настройки
│   └── urls.py         # Главные URL
├── static/             # Статические файлы
│   ├── css/            # Стили
│   └── js/             # JavaScript
├── docker/             # Docker конфигурация
│   ├── nginx.conf      # Nginx
│   └── supervisord.conf# Supervisor
├── Dockerfile          # Сборка образа
├── docker-compose.yml  # Compose файл
└── deploy.sh           # Скрипт развертывания
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DJANGO_SECRET_KEY` | Секретный ключ Django | Генерируется |
| `DJANGO_SETTINGS_MODULE` | Модуль настроек | `web_dashboard.settings` |

### Разрешенные хосты

- `dashbord.gegaremant.ru`
- `localhost`
- `127.0.0.1`
- `155.212.166.17`

## 📋 Полезные команды Docker

```bash
# Просмотр логов
docker logs -f web_dashboard

# Остановка
docker stop web_dashboard

# Перезапуск
docker restart web_dashboard

# Удаление
docker rm -f web_dashboard

# Вход в контейнер
docker exec -it web_dashboard sh
```

## 📜 Лицензия

MIT License
